// เลิกใช้แล้ว — ระบบนี้เป็น "มอนิเตอร์เสียงลูกค้า" ไม่ติดตามสถานะ/ส่งต่อเรื่อง (ตัดออกในเฟสนี้)
// คงไฟล์ไว้เป็น stub เปล่า เพื่อไม่ให้ import ที่หลงเหลือบน GitHub ทำให้ build ล้ม — ลบไฟล์นี้ได้เมื่อสะดวก
export default function CaseActions() {
  return null;
}
